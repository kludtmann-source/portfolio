{
  "name": "knut-ludtmann-de",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "license": "UNLICENSED",
  "scripts": {
    "figure": "node scripts/make-placeholder-figure.mjs && node scripts/bake-poses.mjs",
    "predev": "npm run figure",
    "dev": "astro dev",
    "prebuild": "npm run figure",
    "build": "astro build",
    "preview": "astro preview",
    "validate": "node scripts/validate-content.mjs",
    "pretest": "npm run validate",
    "test": "npm run build && playwright test"
  },
  "dependencies": {
    "astro": "^5.13.0",
    "js-yaml": "^4.1.0",
    "three": "^0.180.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0",
    "@types/js-yaml": "^4.0.9",
    "@types/three": "^0.180.0",
    "ajv": "^8.17.1",
    "ajv-formats": "^3.0.1",
    "typescript": "^5.6.3"
  }
}
